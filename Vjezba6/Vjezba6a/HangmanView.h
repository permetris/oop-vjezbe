#include <cstddef>
#include "HangmanModel.h"
#pragma once

class View
{
public:
	void DisplayHangman(Model hangman);
	void DisplayUsedLetters(Model hangman);
	void DisplayCurrentProgress(Model hangman);

private:

};
