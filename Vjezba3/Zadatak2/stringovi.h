#ifndef SRINGS_H
#define STRINGS_H
#include <vector>
#include <string>
#include <iostream>
#include <algorithm>

using namespace std;

int substringCount(const string&, const string& );
void stringVector(vector<string> &);
void printVector(const vector<string> &);


#endif