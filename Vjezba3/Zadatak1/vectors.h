#ifndef VECTORS_H
#define VECTORS_H
#include <vector>

using namespace std;


void vectorInput(vector<int>&, int );
void vectorInputRange(vector<int>&, int, int);
void vectorPrint(const vector<int>&);
void vectorDifference(vector<int>&, vector<int>&, vector<int>&);
void removeValueFromVector(vector<int>&, int);


#endif
